<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="LockIt.Controls.KeyCombinationBox"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LockIt.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Border BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefault}" CornerRadius="5" MinHeight="40" Padding="5">
        <ItemsControl ItemsSource="{x:Bind Items, Mode=OneWay}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="5" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <Border Background="{StaticResource CardStrokeColorDefaultSolidBrush}" Padding="8" MinWidth="40" CornerRadius="5">
                        <TextBlock Text="{x:Bind}" FontSize="18" FontFamily="Cascadia Code" TextAlignment="Center" />
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Border>
</UserControl>
